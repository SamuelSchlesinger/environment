_common: !include "../shared.yaml"

name: scripts
<<: *common

dependencies:
  - base

executables:
  edit-script:
    main: edit-script.hs
    source-dirs: src
    dependencies:
      - text
      - process
      - directory
      - base
      - commander
      - environment-core
    <<: *executable

tests:
  scripts-test:
    main: Test.hs
    source-dirs: test
    dependencies:
      - scripts
    <<: *test
